<div class="postform">
<h3>编辑页面 - <%= link_to(@page.title, page_path(@page.slug)) %></h3>

<%= semantic_form_for([:admin, @page]) do |form| -%>
  <%= render :partial => 'form', :locals => {:form => form} %>
  <%= form.actions do -%>
    <%= form.action :submit, :as => :button, :button_html => {:class => "btn btn-primary"} -%>
  <% end -%>
<% end -%>
</div>
